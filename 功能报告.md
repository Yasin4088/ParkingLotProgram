# 停车场装卸排队管理系统 — 功能报告

> 整理日期：2026-08-11 | 分支：dev-lizirui

---

## 一、系统概述

一个运行在**微信小程序**内的停车场装卸排队管理系统。货车司机预约装卸 → 到场 GPS 签到 → 排队等待 → 管理员叫号并指派叉车司机 → 司机确认 → 叉车司机执行装卸 → 完成任务。整个流程由管理员在叫号看板统一调度，叉车司机独立登录接收任务。

**技术栈**：微信小程序原生 + WeUI + 微信云开发（单云函数 MVC: route → Controller → Service → Model）

---

## 二、用户角色（3 种）

### 1. 货车司机（driver）
- **登录方式**：微信一键登录（首次需完善个人身份证/车牌/证件照等信息）
- **密码存储**：MD5（兼容历史，QueueService.driverLogin）
- **权限控制**：`CACHE_TOKEN`（24h TTL）

### 2. 叉车司机（forklift）
- **登录方式**：用户名 + 密码（登录页"叉车司机"Tab）
- **密码存储**：bcrypt
- **账号创建**：管理员在后台管理 → 叉车司机管理 → 添加
- **权限控制**：`CACHE_FORKLIFT`（24h TTL），`ForkliftBiz.isForklift(this)` 守卫
- **导航栏颜色**：`#E67E22`（橙色）

### 3. 管理员（admin）
- **登录方式**：用户名 + 密码（登录页"管理员"Tab）
- **密码存储**：bcrypt + 频率限制（防暴力破解）
- **首次部署**：管理员 Tab 自动检测 → 显示初始化表单 → 创建超级管理员（一次性）
- **管理员类型**：超级管理员（type=1）/ 一般管理员（type=0），超级管理员可管理其他管理员
- **权限控制**：`CACHE_ADMIN`，`AdminBiz.isAdmin(this)` 守卫
- **导航栏颜色**：`#009F72`（绿色）

---

## 三、排队状态机

| 状态码 | 状态名 | 含义 | 触发操作 |
|--------|--------|------|----------|
| 0 | 已预约 (BOOKED) | 司机已提交预约，尚未到场 | 司机提交预约 |
| 1 | 排队中 (WAITING) | 司机已 GPS 签到，进入等待队列 | 司机点击 GPS 签到 |
| 2 | 已叫号 (CALLED) | 管理员已叫号，等待司机确认 | 管理员叫号 + 指派叉车 |
| 3 | 司机已确认 (CONFIRMED) | 司机确认收到，叉车司机可见任务 | 司机点击确认收到 |
| 9 | 已完成 (DONE) | 装卸完成 | 叉车司机/管理员点完成 |
| 10 | 已取消 (CANCEL) | 被取消 | 管理员主动取消 或 系统超时自动取消 |

**超时规则**：
- 已预约 24h 未签到 → 自动取消
- 已叫号 5min 未确认 → 自动取消（需重新预约）

---

## 四、各角色完整工作流

### 货车司机

```
登录（微信一键）
  └─ 首次 → 完善个人资料（身份证、车牌号、驾驶证照、行驶证照、身份证照）
       └─ 预约装卸（drivers/home）
            ├─ 选择业务类型：装货 / 卸货
            ├─ 填写车牌号、手机号、货物名称
            └─ 上传单证照片 → 提交 → 状态：已预约
                 └─ 到现场 → 排队状态页（drivers/queue）
                      └─ 点击 GPS 签到 → 状态：排队中（含排队号、前方人数）
                           └─ 管理员叫号后 → 收到叉车司机姓名
                                └─ 点击"确认收到" → 状态：司机已确认
                                     └─ 等待叉车司机完成装卸
```

### 管理员

```
登录（用户名+密码）
  └─ 排队叫号看板（admin/queue）—— 核心操作界面
       ├─ 查看队列：已预约 + 排队中 + 已叫号 + 已确认 全部显示
       ├─ 对"排队中"的车辆 → 点击"叫号"
       │    └─ 弹出叫号弹窗 → 选择叉车司机 → 确认叫号
       │         └─ 状态变为"已叫号"，显示叉车司机姓名
       ├─ 对"已确认"的车辆 → 点击"完成装卸"（手动完成）
       ├─ 点击车辆卡片 → 详情弹窗
       │    ├─ 查看完整信息（车牌、手机、业务、货物、叉车司机、时间线、单证照片）
       │    ├─ 编辑成员信息
       │    └─ 删除并取消预约（需填写原因）
       ├─ 导航入口：司机管理、叉车管理、后台管理
       └─ 刷新按钮

  └─ 后台管理（pages/admin/index/home/admin_home）
       ├─ 用户管理（所有微信登录用户）
       ├─ 司机管理（admin/driver/list）→ 增删改查司机账号
       ├─ 叉车司机管理（admin/forklift/list）→ 增删改查叉车司机账号
       ├─ 管理员管理（仅超级管理员可见）
       ├─ 操作日志
       ├─ 内容管理 / 活动管理（CCMiniCloud 框架原有功能）
       └─ 关于我们 / 联系我们 / 小程序二维码 编辑
```

### 叉车司机

```
登录（用户名+密码）
  └─ 任务页（pages/forklift/home）
       ├─ 暂无任务 → 显示空状态
       ├─ 有任务时 → 显示任务详情卡片
       │    ├─ 车牌号、货物名称、业务类型、手机号
       │    ├─ 叫号时间、司机确认时间
       │    └─ "完成任务"按钮
       └─ 点击"完成任务" → 确认弹窗 → 任务消失，管理员端同步
```

---

## 五、页面清单（13 页）

### 通用
| 页面 | 路径 | 说明 |
|------|------|------|
| 登录 | pages/login/login | 三 Tab：司机（微信登录）/ 叉车司机（账号密码）/ 管理员（账号密码）+ 系统初始化 |

### 司机端
| 页面 | 路径 | 说明 |
|------|------|------|
| 预约 | driver/home | 选业务类型、填车牌/手机/货物名称、上传单证、提交预约 |
| 排队 | driver/queue | 查看排队状态、GPS 签到、确认收到叫号、订阅通知 |
| 注册 | pages/driver/register/register | 完善个人信息（身份证、车牌、证件照） |

### 叉车司机端
| 页面 | 路径 | 说明 |
|------|------|------|
| 任务 | pages/forklift/home | 查看指派任务、完成任务 |

### 管理员端
| 页面 | 路径 | 说明 |
|------|------|------|
| 叫号看板 | admin/queue | **核心操作页**：查看所有队列、逐车叫号+指派叉车、详情弹窗、编辑、取消 |
| 后台首页 | pages/admin/index/home/admin_home | 功能导航（统计面板 + 各管理入口） |
| 司机列表 | pages/admin/driver/list | 司机账号 CRUD |
| 司机编辑 | pages/admin/driver/edit | 新增/编辑司机（用户名/密码/手机/状态 + 查看身份信息/证件照） |
| 叉车司机列表 | pages/admin/forklift/list | 叉车司机账号 CRUD |
| 叉车司机编辑 | pages/admin/forklift/edit | 新增/编辑叉车司机（用户名/密码/手机/状态） |
| 管理员列表 | pages/admin/mgr/admin/list | 管理员账号管理（超管专属） |
| 管理员编辑 | pages/admin/mgr/admin/edit | 新增/编辑管理员 |

（另有 CCMiniCloud 框架自带的活动管理、内容管理、日志等页面，不再逐一列举）

---

## 六、API 路由清单（25 条）

### 队列相关（queue/）
| 路由 | 方法 | 说明 |
|------|------|------|
| queue/options | queue_controller@options | 获取业务选项（堆场、装卸类型） |
| queue/create | queue_controller@create | 司机提交预约 |
| queue/my_current | queue_controller@myCurrent | 司机查当前排队状态 |
| queue/checkin | queue_controller@checkin | 司机 GPS 签到 |
| queue/subscribe | queue_controller@subscribe | 司机订阅叫号通知 |
| queue/confirm | queue_controller@confirm | 司机确认收到叫号 |

### 司机相关（driver/）
| 路由 | 方法 | 说明 |
|------|------|------|
| driver/wxlogin | driver_controller@wxLogin | 微信一键登录 |
| driver/register | driver_controller@register | 完善个人信息 |
| driver/getphonenumber | driver_controller@getPhoneNumber | 获取微信手机号 |
| driver/getinfo | driver_controller@getMyDetail | 获取个人信息 |
| driver/updateinfo | driver_controller@updateInfo | 更新个人信息 |

### 叉车司机相关（forklift/）
| 路由 | 方法 | 说明 |
|------|------|------|
| forklift/login | forklift_controller@login | 叉车司机登录（bcrypt） |
| forklift/my_task | forklift_controller@myTask | 获取当前任务（仅 CONFIRMED） |
| forklift/my_tasks | forklift_controller@myTasks | 获取任务列表（CALLED + CONFIRMED） |
| forklift/complete | forklift_controller@complete | 完成任务（CONFIRMED→DONE） |

### 管理员端（admin/）
| 路由 | 方法 | 说明 |
|------|------|------|
| admin/login | admin_home_controller@adminLogin | 管理员登录 |
| admin/check_setup | admin_home_controller@checkSetup | 检查是否需要初始化 |
| admin/home | admin_home_controller@adminHome | 后台首页统计 |
| admin/clear_cache | admin_home_controller@clearCache | 清除缓存 |
| admin/queue_list | admin_queue_controller@list | 队列列表 |
| admin/queue_call_next | admin_queue_controller@callNext | 叫号（指定车辆+叉车司机） |
| admin/queue_detail | admin_queue_controller@detail | 查看详情 |
| admin/queue_edit | admin_queue_controller@edit | 编辑预约信息 |
| admin/queue_cancel | admin_queue_controller@cancel | 取消排队 |
| admin/queue_finish | admin_queue_controller@finish | 手动完成 |
| admin/forklift_list | admin_queue_controller@forkliftList | 获取可用叉车司机列表 |
| admin/user_list | admin_user_controller@getUserList | 用户列表（支持 role 参数） |
| admin/user_detail | admin_user_controller@getUserDetailById | 用户详情 |
| admin/user_insert | admin_user_controller@insertUser | 新增用户（驱动/叉车） |
| admin/user_edit | admin_user_controller@editUser | 编辑用户 |
| admin/user_del | admin_user_controller@delUser | 删除用户 |
| admin/setup | admin_mgr_controller@setupAdmin | 初始化超级管理员 |
| admin/admin_\* | admin_mgr_controller@\* | 管理员 CRUD + 改密/重置密码 |

---

## 七、数据库集合（NoSQL，微信云数据库）

### ax_user（用户表）
存储所有用户（司机、叉车司机、管理员），通过 `USER_ROLE` 字段区分。

**角色取值**：`driver` / `forklift` / `admin`

**关键字段**：
- `USER_NAME` 用户名（司机为微信昵称）
- `USER_PASSWORD` 密码（司机 MD5，叉车/管理员 bcrypt）
- `USER_MOBILE` 手机号
- `USER_ROLE` 角色标识
- `USER_STATUS` 状态（0=待审核, 1=正常, 9=已禁用）
- `USER_IDCARD` / `USER_LICENSE_PLATE` / `USER_*_IMG` 等司机身份信息
- `USER_LOGIN_CNT` / `USER_LOGIN_TIME` 登录统计

### ax_queue（排队记录表）
存储每条预约/排队记录，字段前缀 `QUEUE_`。

**新字段（本分支新增）**：
- `QUEUE_FORKLIFT_ID` 叉车司机用户 ID
- `QUEUE_FORKLIFT_NAME` 叉车司机姓名
- `QUEUE_FORKLIFT_TIME` 指派时间
- `QUEUE_CONFIRM_TIME` 司机确认时间

---

## 八、技术细节

### 认证机制
| 角色 | 密码方案 | Token | Session 存储 |
|------|----------|-------|-------------|
| 司机 | MD5（历史兼容） | `_id` | `CACHE_TOKEN`（localStorage）|
| 叉车司机 | bcrypt | `_id` | `CACHE_FORKLIFT`（localStorage）|
| 管理员 | bcrypt + 频率限制 | `_id` | `CACHE_ADMIN`（localStorage）|

### 场地
- 系统仅一个场地（硬编码）：`装卸堆场`（ID=A，地址=园区装卸区）
- 所有端都不需要停车场选择

### 前端 Token 传递（cloud_helper.js）
```
route 以 'admin/' 开头  → 读取 CACHE_ADMIN.token
route 以 'forklift/' 开头 → 读取 CACHE_FORKLIFT.id
其他                       → 读取 CACHE_TOKEN.id
```

### 组件
- `cmpt-comm-list` — 通用列表组件（分页、搜索、排序）
- `cmpt-picker` — 选择器组件
- `cmpt-modal` — 弹窗组件
- `pageHelper` — 页面工具（URL 路由、Toast、确认弹窗、列表合并）

### 管理员安全加固（本分支已有特性）
- MD5 → bcrypt（管理员和叉车司机密码）
- 移除硬编码默认凭证
- 登录频率限制（防暴力破解）
- 管理员 CRUD 完整管理

---

## 九、部署步骤

1. 微信开发者工具打开项目根目录
2. 云函数 `cloudfunctions/cloud/` → 右键上传并部署：云端安装依赖
3. 首次使用：登录页 → 管理员 Tab → 初始化表单创建超级管理员
4. 后续：管理员后台 → 添加司机/叉车司机账号 → 投入使用

---

## 十、当前分支状态

- **分支**：`dev-lizirui`（从 `master` 创建）
- **本轮改动总结**：新增叉车司机角色 + 叫号流程改造（灵活叫号 + 叉车司机指派 + 司机确认 + 任务执行）
- **合并前注意**：与合作者确认，避免覆盖云环境共享数据
